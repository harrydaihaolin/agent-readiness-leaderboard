requests>=2.33.1
# v3.2 cohort snapshot uses rules pack v1.5.0 (38 checks; recalibrates
# repo_shape.large_files thresholds 500→1500 lines, 50→150 KB to land
# fire rate inside the 30-60% discriminative band). Keep the major-
# pinned upper bound so a future v2 can't auto-upgrade the daily scan
# into uncharted territory.
agent-readiness>=1.5.0,<2
