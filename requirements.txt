requests>=2.33.1
# v3.2 cohort snapshot uses rules pack v1.5.0 (38 checks; recalibrates
# repo_shape.large_files thresholds 500→1500 lines, 50→150 KB to land
# fire rate inside the 30-60% discriminative band). agent-readiness
# 2.1.x adds the EXP-1 "action contract" (Finding.action, Finding.verify)
# as additive fields — scores.schema.json's TopFinding sets
# additionalProperties=true so the envelope still validates if scan.py
# starts surfacing them. Per CHANGELOG (2.1.0) "Notes for downstream":
# leaderboard should pin >=2.1,<3. The 3.0 ceiling guards against the
# next major auto-upgrading the daily scan into uncharted territory.
agent-readiness>=2.1,<3
